/**
 * Interface used to construct a DTSFormatter instance.
 *
 * @public
 */
export interface Options {
  /**
   * The characters to append to the beginning and end of processed Entry
   * instances.
   */
  caps?: Array<string>;
}
